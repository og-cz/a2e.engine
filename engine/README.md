# A2E Engine

This directory contains the Phase 1 engine library. It has no game-specific entities or autonomous-agent logic. Platform-specific window code is isolated in `src/win32_window.cpp` behind the interfaces in `include/a2e/window.hpp`.
